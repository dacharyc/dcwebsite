{
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:agent-docs": "vitest run tests/agent-docs.test.ts"
  },
  "devDependencies": {
    "afdocs": "^0.9.2",
    "vitest": "^4.0.0"
  }
}
